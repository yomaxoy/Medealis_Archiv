# =============================================================================
# Medealis Warehouse Management System - Core Dependencies
# =============================================================================
# Basis-Installation für Produktiv-Betrieb
# Installation: pip install -r requirements.txt
# =============================================================================

# Core UI & Document Processing
python-docx>=0.8.11
Pillow>=10.4.0
python-barcode[images]>=0.15.0

# Streamlit Web Framework
streamlit>=1.28.0
streamlit-cookies-manager>=0.2.0
requests>=2.28.0

# Database & Security
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9     # PostgreSQL Driver für Production
bcrypt>=4.0.0              # Passwort-Hashing für User-Authentifizierung

# AI Services
anthropic>=0.25.0          # Claude AI für intelligente Dokumentenverarbeitung
httpx>=0.24.0              # HTTP Client für Anthropic

# PDF Processing
PyPDF2>=3.0.1
PyMuPDF>=1.23.0

# PDF Conversion (DOCX to PDF)
pywin32>=306; platform_system=="Windows"  # win32com für Word-Automation
docx2pdf>=0.1.8                            # Plattformunabhängige Alternative

# Utilities
appdirs>=1.4.4
cryptography>=41.0.7
python-dotenv>=1.0.0       # .env Datei-Unterstützung für Konfiguration

# Optional: OpenAI (falls Claude nicht verfügbar)
openai>=1.3.0

# =============================================================================
# Optionale Features - siehe separate Requirements-Dateien:
# - requirements-dev.txt       Development-Tools (pytest, black, etc.)
# - requirements-ocr.txt       OCR-Feature für Lieferschein-Scanning
# - requirements-service.txt   Windows Service (Server-Deployment)
# =============================================================================
